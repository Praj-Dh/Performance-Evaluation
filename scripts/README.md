# Scripts

Run all scripts from the **project root**.

| Script | Description |
|--------|-------------|
| `./scripts/dev.sh` | Start local dev: MySQL (if Homebrew), PHP backend on :8080, Next.js on :3000. Logs to `.dev-logs/`. |
